pub struct Config {
    pub min_protocol_basefee: u128,
    pub max_tx_count: u64,
    pub max_tx_per_account: u64,
    pub max_pool_size: u64,
    pub block_gas_limit: u64,
    pub price_bump_percentage: u128,
}
